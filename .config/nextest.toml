# Test runner configuration (https://nexte.st).

[profile.default]
# Keep going after a failure so every test binary reports, including integration and
# property tests behind a failing unit test.
fail-fast = false

[profile.ci]
fail-fast = false
failure-output = "immediate-final"
